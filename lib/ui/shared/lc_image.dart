import 'package:flutter/material.dart';

class LCImage extends AssetImage {
  final String image;
  const LCImage(this.image) : super(image);

  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
